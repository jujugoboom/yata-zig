name: Test commit
on:
  push:
    branches:
      - "*"

jobs:
  deploy:
    runs-on: docker
    steps:
      - uses: actions/checkout@v4
      - name: install zig
        run: |
          wget https://ziglang.org/download/0.15.1/zig-x86_64-linux-0.15.1.tar.xz
          tar xf zig-x86_64-linux-0.15.1.tar.xz
          cp zig-x86_64-linux-0.15.1/zig /usr/bin/
      - name: Run tests
        run: zig build test
